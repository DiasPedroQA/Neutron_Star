name: CI-CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  lint:
    name: Lint & Formatação (Ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Atoms/pyproject.toml
      - name: Instalar dependências de lint (lock)
        working-directory: Atoms
        run: python -m pip install --require-hashes -r requirements-dev.lock
      - name: Ruff check
        working-directory: Atoms
        run: python -m ruff check src tests
      - name: Ruff format check
        working-directory: Atoms
        run: python -m ruff format --check src tests

  security:
    name: Auditoria de Segurança
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Atoms/pyproject.toml
      - name: Instalar dependências de segurança (lock)
        working-directory: Atoms
        run: python -m pip install --require-hashes -r requirements-security.lock
      - name: Bandit
        working-directory: Atoms
        run: python -m bandit -c pyproject.toml -r src
      - name: pip-audit (ambiente instalado)
        working-directory: Atoms
        run: python -m pip_audit

  test:
    name: Testes (${{ matrix.os }}, Python ${{ matrix.python-version }})
    needs: lint
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Configurar Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: Atoms/pyproject.toml
      - name: Instalar dependências (lock completo, inclui build)
        working-directory: Atoms
        run: python -m pip install --require-hashes -r requirements.lock
      - name: Construir wheel do projeto
        working-directory: Atoms
        run: python -m build --wheel --outdir dist
      - name: Instalar o projeto (sem dependências)
        working-directory: Atoms
        run: python -m pip install --no-deps dist/*.whl
      - name: Executar testes com cobertura
        working-directory: Atoms
        run: python -m pytest --cov=src --cov-report=term-missing --cov-report=xml
      - name: Upload da cobertura (apenas Ubuntu + Python 3.12)
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: Atoms/coverage.xml

  build-check:
    name: Verificação de Build (wheel + sdist)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Atoms/pyproject.toml
      - name: Instalar ferramentas de build (lock)
        working-directory: Atoms
        run: python -m pip install --require-hashes -r requirements-build.lock
      - name: Construir distribuições
        working-directory: Atoms
        run: python -m build
      - name: Conferir artefatos gerados
        working-directory: Atoms
        run: |
          ls -la dist/
          test -f dist/*.whl || (echo "❌ Wheel não encontrado" && exit 1)
          test -f dist/*.tar.gz || (echo "❌ Source tarball não encontrada" && exit 1)
          echo "✅ Build bem-sucedido"

  docs:
    name: Documentação (Build Sphinx)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Atoms/pyproject.toml
      - name: Instalar dependências de docs (lock, inclui build)
        working-directory: Atoms
        run: python -m pip install --require-hashes -r requirements-docs.lock
      - name: Construir wheel do projeto
        working-directory: Atoms
        run: python -m build --wheel --outdir dist
      - name: Instalar o projeto (sem dependências)
        working-directory: Atoms
        run: python -m pip install --no-deps dist/*.whl
      - name: Build Sphinx (warnings como erros)
        working-directory: Atoms
        run: python -m sphinx -b html docs docs/_build/html -W

  sonar:
    name: SonarCloud Analysis
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Baixar cobertura
        uses: actions/download-artifact@v4
        with:
          name: coverage
          path: Atoms
      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@2f77a1ec69fb1d595b06f35ab27e97605bdef703
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
