# charset-normalizer: usado para detecção/correção de encoding de texto (útil ao ler arquivos HTML/JSON em vários formatos).
charset-normalizer==3.4.7

# pillow: biblioteca para manipulação de imagens em Python (pode ser usada em exportação/relatórios que lidem com ícones ou thumbnails).
pillow==12.2.0

# reportlab: geração de PDFs em alto nível (relatórios, documentos complexos). Pode complementar ou substituir fpdf2 em alguns casos.
reportlab==5.0.0

# beautifulsoup4: parser HTML/XML, muito usado para extrair dados de arquivos de bookmarks ou páginas web.
beautifulsoup4>=4.12.2

# fpdf2: biblioteca leve para gerar PDFs via código (títulos, parágrafos, tabelas etc.), usada pelos exportadores PDF.
fpdf2>=2.5.4
