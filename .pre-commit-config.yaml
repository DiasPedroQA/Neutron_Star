# .pre-commit-config.yaml

default_language_version:
  python: python3.12

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.6.0
  #   hooks:
  #     - id: ruff
  #       args: [--fix]
  #     - id: ruff-format

  # - repo: local
  #   hooks:
  #     - id: pytest
  #       name: pytest
  #       entry: bash -c 'cd Atoms && PYTHONPATH=src python -m pytest tests -q --tb=no'
  #       language: system
  #       pass_filenames: false
  #     - id: mypy
  #       name: mypy
  #       entry: bash -c 'cd Atoms && PYTHONPATH=src python -m mypy src tests main.py --ignore-missing-imports'
  #       language: system
  #       pass_filenames: false
