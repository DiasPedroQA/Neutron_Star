repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.0  # Verifique a versão mais recente
    hooks:
      - id: ruff
        args: [ --fix ]
      - id: ruff-format  # similar ao black, mas via ruff

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.10.0
    hooks:
      - id: mypy
        additional_dependencies: [types-requests]
        args: [--ignore-missing-imports]

  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: .venv/bin/pytest
        language: script
        pass_filenames: false
        always_run: true